[package]
name = "flint-connect"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
sysinfo = { workspace = true, features = ["system", "multithread"] }
serde = { workspace = true, features = ["derive"] }
