[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "flint-protocol"
version = "0.1.0"
requires-python = ">=3.7"
dependencies = ["protobuf==4.24.4"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["flint_protocol*"]
